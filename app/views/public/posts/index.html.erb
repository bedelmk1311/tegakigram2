<div class='container'>
  <div class ="text-center">
    <%=  link_to posts_path, class:"btn btn-outline-secondary" do %>
      <i class="fa-solid fa-house fa-sm" ></i>ホーム
    <% end %>
      <%= render 'layouts/btn_index' %>
  </div>
  <%= render 'public/posts/index', posts: @posts %>
  <%= paginate @posts %>
</div>